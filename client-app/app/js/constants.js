
/*const reloadButton = document.getElementById("reloadButton")

const textInput = document.getElementById("textInput")
const backgroundColor = document.getElementById("backgroundColor")
const textColor = document.getElementById('textColor')
const displayTime = document.getElementById("displayTime")
const textSize = document.getElementById("textSize")
*/

//const portListBox = document.getElementById("portList")
const screenPreview = document.getElementById("screenPreview")
const screenPreview2Send = document.getElementById("screenPreview2Send")
const screenDataButton = document.getElementById('screenDataButton')

const screenWidth = 240
const screenHeight = 135

const ctx = screenPreview.getContext("2d")
const ctx2Send = screenPreview2Send.getContext("2d")
